name: Test JM

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from GitHub Actions!"
      - run: pip install jmcomic 2>&1 | tail -3
      - run: python3 -c "from jmcomic import download_album; print('jmcomic OK')"
